// Single source for the Stop-hook line-count thresholds, shared by both hooks.
export const REMIND_LINES = 30; // >= this nudges the agent toward lotra
export const GATE_LINES = 80; // >= this forces a lotra review round
